function calculate(a=3, b=1){   //default variables or local variable.
    //or local variable and the value wont return
    //will be replaced when assigned
let sum = a + b;
return sum;
}

console.log(calculate(8,10));//assigning variables using parameter