const prompt = require("prompt-sync")();


let a, b; 
// this is Global variable where written  outside the function and we can access it from erverwher


function addition(){
    console.log(a , b);
    let c;
    let d;
    // this is local variable
}
